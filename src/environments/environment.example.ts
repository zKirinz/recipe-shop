/* 
This file is an example for properties of `environment.prod.ts` and `environment.prod.ts` file
you need to create and replace suitable value of properties so as to make application run correctly.
*/

export const environment = {
  production: false,
  firebaseAPIKey: 'your_api_key',
};
